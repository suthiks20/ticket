// Declare asynchronous sales persistence operations for future implementation.
'use strict';

/** Create or replace sale state for the requested ticket inventory. */
async function reset() {}

/** Read the currently active sale record. */
async function getActive() {}

module.exports = { reset, getActive };
