// Shared sales module table names and response messages.
'use strict';

module.exports = Object.freeze({
  TABLE_NAMES: Object.freeze({ sales: 'sales' }),
  ERROR_MESSAGES: Object.freeze({ invalidTicketCount: 'Invalid ticket_count' })
});
